package ru.examples.bilimdonuz.utils

import android.content.Context
import android.content.SharedPreferences
import androidx.preference.PreferenceManager
import com.google.gson.Gson
import com.google.gson.reflect.TypeToken
import ru.examples.bilimdonuz.model.AnswerModel
import java.lang.reflect.Type


/*
class TextAnswerPrefernse (
    private val pereferense:SharedPreferences,
    private val key:String,
    private val value:String=""
MutableList<AnswerModel>

):ReadWriteProperty<Any,MutableList<AnswerModel>>{
    override fun setValue(thisRef: Any, property: KProperty<*>, value: MutableList<AnswerModel>) {
         pereferense.edit().pu
    }

    override fun getValue(thisRef: Any, property: KProperty<*>): MutableList<AnswerModel> {

    }
}
*/

